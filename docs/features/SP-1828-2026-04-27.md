---
title: "SP-1828 - SP-1828-Expected-Payout-Date-for-Internal-Team"
date: 2026-04-27
author: github-actions[bot]
pr_number: 1099
jira_ticket: SP-1828
---

## 📌 PR Overview

* Title: SP-1828-Expected-Payout-Date-for-Internal-Team
* Type: feature
* Scope: small

---

## 🧠 Summary of Changes

* Bullet points of ACTUAL changes (not generic)

---

## 🏗️ Architecture Impact

* Layers Modified: Prisma, DTOs, Services, etc.
* Flow Impact: Controller → Service → Repository → DB, as far as the diff shows

---

## 📂 Detailed Changes

### Prisma Models & DB (migrations + schema.prisma)

* List added/updated columns/fields with SQL/Prisma names
* Defaults, nullability, and purpose where inferable

### DTOs

* New/changed fields, decorators/validation if visible
* Impact on API input shape

### Services

For each touched service file:

* Conditions/branches added or changed
* Methods or calls introduced (name them)
* BEFORE vs AFTER behavior

### Controllers

* Explicitly state "No changes" if the diff shows none

### Repositories

* Explicitly state "No changes" if the diff shows none

### Helpers

* Explicitly state "No changes" if the diff shows none

---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1828-Expected-Payout-Date-for-Internal-Team

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260406120000_update_show_expected_payout_date_system_config/migration.sql
--- /dev/null
+++ b/database/migrations/20260406120000_update_show_expected_payout_date_system_config/migration.sql
@@ -0,0 +1,12 @@
+-- Express sale accordion: expected payout date and upon-collection toggles (JSON config)
+UPDATE system_configs
+SET
+    type = 'json',
+    value = json_build_object(
+        'showExpectedPayoutDate', false,
+        'showUponCollection', false,
+        'uponCollectionLabel', 'Once Collected'
+    )::text,
+    updated_at = NOW(),
+    description = 'Express sale accordion: toggle expected payout date, toggle upon-collection label, and custom upon-collection label text.'
+WHERE key = 'SHOW_EXPECTED_PAYOUT_DATE';
```

## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260406120000_update_show_expected_payout_date_system_config/migration.sql b/database/migrations/20260406120000_update_show_expected_payout_date_system_config/migration.sql
new file mode 100644
index 00000000..819e0d1f
--- /dev/null
+++ b/database/migrations/20260406120000_update_show_expected_payout_date_system_config/migration.sql
@@ -0,0 +1,12 @@
+-- Express sale accordion: expected payout date and upon-collection toggles (JSON config)
+UPDATE system_configs
+SET
+    type = 'json',
+    value = json_build_object(
+        'showExpectedPayoutDate', false,
+        'showUponCollection', false,
+        'uponCollectionLabel', 'Once Collected'
+    )::text,
+    updated_at = NOW(),
+    description = 'Express sale accordion: toggle expected payout date, toggle upon-collection label, and custom upon-collection label text.'
+WHERE key = 'SHOW_EXPECTED_PAYOUT_DATE';
```

## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

(no changes in this PR)

## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1099)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1828)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
