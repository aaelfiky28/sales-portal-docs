---
title: "SP-1852 - SP-1852 Referral Org Payments"
date: 2026-04-26
author: github-actions[bot]
pr_number: 1146
jira_ticket: SP-1852
---

The provided code snippet is a part of an authentication service that handles organization referral exports. It includes several helper functions and classes to manage the export process, such as creating a new audit record for each export operation.

Here's a breakdown of what this code does:

1. **Imports**: The script imports necessary modules from `@nestjs/common`, `prisma-client`, and `src/prisma/unit-of-work`.

2. **Constants**:
   - `ADMIN_GROUPS`: An array containing the names of security groups that are considered administrators for collection agents.
   - `isCollectionAgent` function: Checks if a user is an admin by verifying their group membership in the `ADMIN_GROUPS` array.

3. **Export DTOs**:
   - `export-organization-referral.dto.ts`: Defines the structure of the export request, including optional boolean flags to include or exclude eligible exports.
   - `OrganizationReferralDto.ts`: Defines the structure of an organization referral export request, which includes a list of organizations and their statuses.

4. **UnitOfWork Class**:
   - `export-organization-referral-export-audit.repository.ts`: Manages the creation of audit records for each export operation using a unit-of-work client from `@nestjs/prisma`.

5. **Helper Functions**:
   - `isSuperAdmin` function: Checks if a user is an admin by verifying their group membership in the `ADMIN_GROUPS` array.
   - `isAdminGroups`: Returns an array of security groups that are considered administrators for collection agents.

6. **Export Functionality**:
   - The `exportOrganizationReferral` function takes an export request as input and returns a promise that resolves to the exported file path or error message if there is an issue.
   - It uses the `create` method of the `OrganizationReferralExportAuditRepositoryService` class to create a new audit record for each export operation.

This code provides a basic framework for managing organization referral exports, including creating and updating audit records. You can expand upon this by adding more features such as error handling, logging, or integration with other services like AWS S3.
## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1146)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1852)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
