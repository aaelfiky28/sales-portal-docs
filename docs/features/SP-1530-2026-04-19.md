---
title: "SP-1530 - SP-1530-collection-team"
date: 2026-04-19
author: github-actions[bot]
pr_number: 1058
jira_ticket: SP-1530
---

## Changes Made

1. **DTOs (`QueryCollectionCycleSales`)**:
    - Added `activeTab`, `salesCollectionAgentUids`, and `userUid` fields to the DTO.
    - Updated the constructor to accept these new fields.

2. **Repository Service (`CollectionCycleRepositoryService`)**:
    - Implemented methods to construct a `Prisma.CollectionCycleWhereInput` based on the provided query parameters.
    - Added validation decorators for optional fields like `activeTab`, `salesCollectionAgentUids`, and `userUid`.

3. **Sales Service (`SalesService`)**:
    - Updated the DTOs to include new properties: `headOfSales`, `partner`, and `salesCollectionAgentUids`.
    - Implemented methods to construct a `CommonQueryOptionsDto` based on the provided query parameters.
    - Added validation decorators for optional fields like `headOfSales`, `partner`, and `salesCollectionAgentUids`.

4. **Helpers (`*Helper.ts`)**:
    - Updated the DTOs to include new properties: `activeTab`, `salesCollectionAgentUids`, and `userUid`.
    - Implemented methods to construct a `CommonQueryOptionsDto` based on the provided query parameters.
    - Added validation decorators for optional fields like `headOfSales`, `partner`, and `salesCollectionAgentUids`.

## Summary

- **DTOs**: Updated to include new properties: `activeTab`, `salesCollectionAgentUids`, and `userUid`.
- **Repository Service**: Implemented methods to construct a `Prisma.CollectionCycleWhereInput` based on the provided query parameters.
- **Sales Service**: Updated DTOs to include new properties: `headOfSales`, `partner`, and `salesCollectionAgentUids`.
- **Helpers**: Updated DTOs to include new properties: `activeTab`, `salesCollectionAgentUids`, and `userUid`.

These changes ensure that the data model is more flexible and easier to manage, especially for complex queries.
## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1058)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1530)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
