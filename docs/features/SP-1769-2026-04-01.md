---
title: "SP-1769 - SP-1769-Hold-Payment-for-Specific-Sales"
date: 2026-04-01
author: github-actions[bot]
pr_number: 1054
jira_ticket: SP-1769
---

## 📌 PR Overview

* Title: SP-1769-Hold-Payment-for-Specific-Sales
* Type: feature / bugfix / refactor
* Scope: small / medium / large

---

## 🧠 Summary of Changes

* Bullet points of ACTUAL changes (not generic)

---

## 🏗️ Architecture Impact

* Layers Modified: (explicit list: Prisma, DTOs, Services, etc.)
* Flow Impact: (what changed along Controller → Service → Repository → DB, as far as the diff shows)

---

## 📂 Detailed Changes

### Prisma Models & DB (migrations + schema.prisma)

* List added/updated columns/fields with SQL/Prisma names
* Defaults, nullability, and purpose where inferable

### DTOs

* New/changed fields, decorators/validation if visible
* Impact on API input shape

### Services

For each touched service file:

* Conditions/branches added or changed
* Methods or calls introduced (name them)
* BEFORE vs AFTER behavior

### Controllers

* Explicitly state "No changes" if the diff shows none

### Repositories

* Explicitly state "No changes" if the diff shows none

### Helpers

* Explicitly state "No changes" if the diff shows none

---

## 🔄 Data Flow Changes

Explain how data flow changed across: Controller → Service → Repository → DB (only what the diff supports).

---

## ⚠️ Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1769-Hold-Payment-for-Specific-Sales

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260317233855_add_hold_payment_flag_and_comment/migration.sql
database/migrations/20260329130956_add_release_comment/migration.sql
database/schema/schema.prisma
database/sql/insertInMaterializedViewTable.sql
src/express-eligibility/express-eligibility.service.ts
src/factoring/express-factoring/express-factoring.service.ts
src/factoring/factoring.module.ts
src/partner-express/eligibility/partner-express-eligibility.service.ts
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/sales/dto/update-sale.dto.ts
src/sales/sales.service.ts
src/utils/helpers/file-streamer/parser-factory.ts
src/utils/slack/slack.service
src/utils/logger-handler-service.ts
src/auth/entities/user-token.entity
src/users/users.service
src/partner-express/eligibility/partner-express-eligibility.service.ts

---

## 📝 Changes to Prisma Schema

### Prisma schema changes:

1. **Add `holdPayment` field to the `sales` table**: This is a new field that will be used to track whether a sale has been paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to DTOs

### DTO changes:

1. **Add `holdPayment` field to the `updateSaleDto` interface**: This is a new field that will be used to track whether a sale has been paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Service

### Service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Repository

### Repository changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Helper Files

### Helper files changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logging Service

### Logging service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Slack Service

### Slack service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to User Token Entity

### User token entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to User Service

### User service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `PartnerExpressEligibilityService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Sales Service

### Sales service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Utils File

### Utils file changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Logger Handler Service

### Logger handler service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `SalesService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Auth Entity

### Auth entity changes:

1. **Add `holdPayment` field to the `UserTokenEntity` interface**: This is a new field that will be used to track whether a user has paid for hold.
2. **Update `saleCommission` model**: Add a new field `CurrentPayment` to store the current payment amount, which can be used to calculate the total payment for each sale.

---

## 📝 Changes to Users Service

### Users service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findOne` method in `UserService`:** Add a new condition to filter out sales that are on payment hold.

---

## 📝 Changes to Partner Express Eligibility Service

### Partner express eligibility service changes:

1. **Add `removeUnpaidExpressRequestsForSale` method**: This method will remove express requests for agents who have paid for hold.
2. **Update `findMany` method in `
## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1054)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1769)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
