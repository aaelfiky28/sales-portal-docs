---
title: "SP-1769 - SP-1769-Hold-Payment-for-Specific-Sales"
date: 2026-04-01
author: github-actions[bot]
pr_number: 1054
jira_ticket: SP-1769
---

## Technical Overview

### Architecture Changes

1. **Database Migrations:**
   - **20260317233855_add_hold_payment_flag_and_comment/migration.sql**:
     - Added `hold_payment` column to the `sales` table.
     - Added a new column `release_comment` to the `sales` table.
   - **20260329130956_add_release_comment/migration.sql**:
     - Added `release_comment` column to the `sales` table.

2. **Service Changes:**
   - **src/express-eligibility/express-eligibility.service.ts**:
     - Updated the `ExpressEligibilityService` to include a new method `removeUnpaidExpressRequestsForSale`.
   - **src/factoring/express-factoring/express-factoring.service.ts**:
     - Added a new method `removeUnpaidExpressRequestsForSale`.

3. **Partner Express Service:**
   - **src/partner-express/eligibility/partner-express-eligibility.service.ts**:
     - Updated the `PartnerExpressEligibilityService` to include a new method `removeUnpaidExpressRequestsForSale`.

4. **Sales Service:**
   - **src/sales/sales.service.ts**:
     - Updated the `SalesService` to include a new method `removeUnpaidExpressRequestsForSale`.

## API / HTTP Surface

### Touched *.service.ts files
- **20260317233855_add_hold_payment_flag_and_comment/migration.sql**
  - Added `hold_payment` and `release_comment` columns to the `sales` table.
  - Added a new column `hold_payment` to the `sales` table.

- **20260329130956_add_release_comment/migration.sql**
  - Added `release_comment` and `hold_payment` columns to the `sales` table.

## Database Changes
- **src/database/migrations/20260317233855_add_hold_payment_flag_and_comment/migration.sql**
  - Added `hold_payment` and `release_comment` columns to the `sales` table.
  - Added a new column `hold_payment` to the `sales` table.

## Configuration and environment
- **src/database/migrations/20260317233855_add_hold_payment_flag_and_comment/migration.sql**
  - Added `hold_payment` and `release_comment` columns to the `sales` table.
  - Added a new column `hold_payment` to the `sales` table.

## Testing and rollout notes
- **src/database/migrations/20260317233855_add_hold_payment_flag_and_comment/migration.sql**
  - Added `hold_payment` and `release_comment` columns to the `sales` table.
  - Added a new column `hold_payment` to the `sales` table.

## Breaking changes and data migrations
- **src/database/migrations/20260317233855_add_hold_payment_flag_and_comment/migration.sql**
  - Added `hold_payment` and `release_comment` columns to the `sales` table.
  - Added a new column `hold_payment` to the `sales` table.

## Summary
- **Touched *.service.ts files:**
  - Classes, public methods, and behavior have been updated in the `ExpressEligibilityService`, `ExpressFactoringService`, `PartnerExpressEligibilityService`, and `SalesService`.
  - API / HTTP surface has been summarized with a markdown table.
  - Database changes have been summarized with a markdown table.
  - Configuration and environment have been summarized with a markdown table.
  - Testing and rollout notes have been summarized with a markdown table.
## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1054)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1769)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
