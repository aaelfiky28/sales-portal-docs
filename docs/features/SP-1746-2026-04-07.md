---
title: "SP-1746 - SP-1746-Update-Sale-Tier"
date: 2026-04-07
author: github-actions[bot]
pr_number: 1050
jira_ticket: SP-1746
---

## 📌 PR Overview

* Title: SP-1746-Update-Sale-Tier (infer from PR title + diff; be specific)
* Type: feature / bugfix / refactor
* Scope: small / medium / large

---

## 🧠 Summary of Changes

* Bullet points of ACTUAL changes (not generic)

---

## 🏗️ Architecture Impact

* Layers Modified: (explicit list: Prisma, DTOs, Services, etc.)
* Flow Impact: (what changed along Controller → Service → Repository → DB, as far as the diff shows)

---

## 📂 Detailed Changes

### Prisma Models & DB (migrations + schema.prisma)

* List added/updated columns/fields with SQL/Prisma names
* Defaults, nullability, and purpose where inferable

### DTOs

* New/changed fields, decorators/validation if visible
* Impact on API input shape

### Services

For each touched service file:

* Conditions/branches added or changed
* Methods or calls introduced (name them)
* BEFORE vs AFTER behavior

### Controllers

* Explicitly state "No changes" if the diff shows none

### Repositories

* Explicitly state "No changes" if the diff shows none

### Helpers

* Explicitly state "No changes" if the diff shows none

---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1746-Update-Sale-Tier

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
src/sales/dto/update-sale-partner-information.dto.ts
src/sales/response-dtos/sale-partner-information.repsonsedto.ts
src/sales/sales.service.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

(no changes in this PR)


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

diff --git a/src/sales/sales.service.ts b/src/sales/sales.service.ts
index 85dd558f..d2ee0dd7 100644
--- a/src/sales/sales.service.ts
+++ b/src/sales/sales.service.ts
@@ -2645,7 +2645,16 @@ export class SalesService {
       let commissionMultiplier = sale.commissionMultiplier;
       let targetMultiplier = sale.targetMultiplier;
 
-      if (updateSalePartnerInformationDto.organizationId) {
+      if (updateSalePartnerInformationDto.tierId !== undefined) {
+        updatedTierId = updateSalePartnerInformationDto.tierId;
+        if (updatedTierId === AMBASSADORTIERS[0]) {
+          commissionMultiplier = 0.5;
+          targetMultiplier = 0.5;
+        } else {
+          commissionMultiplier = 1;
+          targetMultiplier = 1;
+        }
+      } else if (updateSalePartnerInformationDto.organizationId) {
         const tierId = await this.getTierId(
           updateSalePartnerInformationDto.organizationId,
         );
       }


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

diff --git a/src/sales/dto/update-sale-partner-information.dto.ts b/src/sales/dto/update-sale-partner-information.dto.ts
index 46d7428f..4f806b7b 100644
--- a/src/sales/dto/update-sale-partner-information.dto.ts
+++ b/src/sales/dto/update-sale-partner-information.dto.ts
@@ -13,6 +13,9 @@ export class UpdateSalePartnerInformationDto {
   @OptionalString({})
   organizationId?: string;
 
+  @OptionalString({ description: 'partner tier id' })
+  tierId?: string;
+
   @OptionalNumber({})
   partnerCommissionPercentage?: number;
 


## Helpers (*.helper.ts and */helpers/*.ts)

(no changes in this PR)

## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1050)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1746)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
