---
title: "SP-1747 - SP-1747-Partners-Cycle-Extraction-Collection-Percentage"
date: 2026-04-27
author: github-actions[bot]
pr_number: 1014
jira_ticket: SP-1747
---

## 📌 PR Overview

* Title: SP-1747-Partners-Cycle-Extraction-Collection-Percentage (infer from PR title + diff; be specific)
* Type: feature / bugfix / refactor
* Scope: small / medium / large

---

## 🧠 Summary of Changes

* Bullet points of ACTUAL changes (not generic)

---

## 🏗️ Architecture Impact

* Layers Modified: (explicit list: Prisma, DTOs, Services, etc.)
* Flow Impact: (what changed along Controller → Service → Repository → DB, as far as the diff shows)

---

## 📂 Detailed Changes

### Prisma Models & DB (migrations + schema.prisma)

* List added/updated columns/fields with SQL/Prisma names
* Defaults, nullability, and purpose where inferable

### DTOs

* New/changed fields, decorators/validation if visible
* Impact on API input shape

### Services

For each touched service file:

* Conditions/branches added or changed
* Methods or calls introduced (name them)
* BEFORE vs AFTER behavior

### Controllers

* Explicitly state "No changes" if the diff shows none

### Repositories

* Explicitly state "No changes" if the diff shows none

### Helpers

* Explicitly state "No changes" if the diff shows none

---

## 🔄 Data Flow Changes

Explain how data flow changed across: Controller → Service → Repository → DB (only what the diff supports).

---

## ⚠️ Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1747-Partners-Cycle-Extraction-Collection-Percentage

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/utils/helpers/sale.utils/sale-money.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
new file mode 100644
index 00000000..e5a4d919
--- /dev/null
+++ b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
@@ -0,0 +1,5 @@
+INSERT INTO sales_reasons_ids (id, reason, created_at, updated_at)
+VALUES
+  (19, 'Remaining Upon Collection', NOW(), NOW()),
+  (20, 'Missing Net Amount', NOW(), NOW())
+ON CONFLICT (id) DO NOTHING;


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

diff --git a/src/utils/helpers/sale.utils/sale-money.ts b/src/utils/helpers/sale.utils/sale-money.ts
index 96f148bc..855eed69 100644
--- a/src/utils/helpers/sale.utils/sale-money.ts
+++ b/src/utils/helpers/sale.utils/sale-money.ts
@@ -13,6 +13,7 @@ import {
   freelancerDocumentsClearForPayment,
 } from 'src/sales/partner-payments/helpers/rules';
 import { calculateCollectedPercentage } from './sale-collected-percentage';
+import { PAYMENT_THRESHOLDS } from 'src/sales-reasons/reasons-enum';
 
 /* RULES */
 
@@ -178,9 +179,16 @@ export function getSaleEffectivePaymentPercentage(
 
   const paidCommPercent = paidNet.divide(totalNet).multiply(100).value;
 
-  // CASE A: remaining commission takes precedence
+  // CASE A: has previous payments — use variance when collected > paid
   if (paidCommPercent > 0) {
-    const effectivePercent = Math.ceil(100 - paidCommPercent);
+    const variance = Math.min(100, collectedPercentage) - paidCommPercent;
+    if (variance >= PAYMENT_THRESHOLDS.REMAINING_UPON_COLLECTION_VARIANCE) {
+      return {
+        effectivePercent: variance,
+        effectiveReason: 'remaining',
+      };
+    }
+    const effectivePercent = 100 - paidCommPercent;
     return { effectivePercent, effectiveReason: 'remaining' };
   }
 
---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1747-Partners-Cycle-Extraction-Collection-Percentage

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/utils/helpers/sale.utils/sale-money.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
new file mode 100644
index 00000000..e5a4d919
--- /dev/null
+++ b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
@@ -0,0 +1,5 @@
+INSERT INTO sales_reasons_ids (id, reason, created_at, updated_at)
+VALUES
+  (19, 'Remaining Upon Collection', NOW(), NOW()),
+  (20, 'Missing Net Amount', NOW(), NOW())
+ON CONFLICT (id) DO NOTHING;


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

diff --git a/src/utils/helpers/sale.utils/sale-money.ts b/src/utils/helpers/sale.utils/sale-money.ts
index 96f148bc..855eed69 100644
--- a/src/utils/helpers/sale.utils/sale-money.ts
+++ b/src/utils/helpers/sale.utils/sale-money.ts
@@ -13,6 +13,7 @@ import {
   freelancerDocumentsClearForPayment,
 } from 'src/sales/partner-payments/helpers/rules';
 import { calculateCollectedPercentage } from './sale-collected-percentage';
+import { PAYMENT_THRESHOLDS } from 'src/sales-reasons/reasons-enum';
 
 /* RULES */
 
@@ -178,9 +179,16 @@ export function getSaleEffectivePaymentPercentage(
 
   const paidCommPercent = paidNet.divide(totalNet).multiply(100).value;
 
-  // CASE A: remaining commission takes precedence
+  // CASE A: has previous payments — use variance when collected > paid
   if (paidCommPercent > 0) {
-    const effectivePercent = Math.ceil(100 - paidCommPercent);
+    const variance = Math.min(100, collectedPercentage) - paidCommPercent;
+    if (variance >= PAYMENT_THRESHOLDS.REMAINING_UPON_COLLECTION_VARIANCE) {
+      return {
+        effectivePercent: variance,
+        effectiveReason: 'remaining',
+      };
+    }
+    const effectivePercent = 100 - paidCommPercent;
     return { effectivePercent, effectiveReason: 'remaining' };
   }
 
---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1747-Partners-Cycle-Extraction-Collection-Percentage

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/utils/helpers/sale.utils/sale-money.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
new file mode 100644
index 00000000..e5a4d919
--- /dev/null
+++ b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
@@ -0,0 +1,5 @@
+INSERT INTO sales_reasons_ids (id, reason, created_at, updated_at)
+VALUES
+  (19, 'Remaining Upon Collection', NOW(), NOW()),
+  (20, 'Missing Net Amount', NOW(), NOW())
+ON CONFLICT (id) DO NOTHING;


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

diff --git a/src/utils/helpers/sale.utils/sale-money.ts b/src/utils/helpers/sale.utils/sale-money.ts
index 96f148bc..855eed69 100644
--- a/src/utils/helpers/sale.utils/sale-money.ts
+++ b/src/utils/helpers/sale.utils/sale-money.ts
@@ -13,6 +13,7 @@ import {
   freelancerDocumentsClearForPayment,
 } from 'src/sales/partner-payments/helpers/rules';
 import { calculateCollectedPercentage } from './sale-collected-percentage';
+import { PAYMENT_THRESHOLDS } from 'src/sales-reasons/reasons-enum';
 
 /* RULES */
 
@@ -178,9 +179,16 @@ export function getSaleEffectivePaymentPercentage(
 
   const paidCommPercent = paidNet.divide(totalNet).multiply(100).value;
 
-  // CASE A: remaining commission takes precedence
+  // CASE A: has previous payments — use variance when collected > paid
   if (paidCommPercent > 0) {
-    const effectivePercent = Math.ceil(100 - paidCommPercent);
+    const variance = Math.min(100, collectedPercentage) - paidCommPercent;
+    if (variance >= PAYMENT_THRESHOLDS.REMAINING_UPON_COLLECTION_VARIANCE) {
+      return {
+        effectivePercent: variance,
+        effectiveReason: 'remaining',
+      };
+    }
+    const effectivePercent = 100 - paidCommPercent;
     return { effectivePercent, effectiveReason: 'remaining' };
   }
 
---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1747-Partners-Cycle-Extraction-Collection-Percentage

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/utils/helpers/sale.utils/sale-money.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
new file mode 100644
index 00000000..e5a4d919
--- /dev/null
+++ b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
@@ -0,0 +1,5 @@
+INSERT INTO sales_reasons_ids (id, reason, created_at, updated_at)
+VALUES
+  (19, 'Remaining Upon Collection', NOW(), NOW()),
+  (20, 'Missing Net Amount', NOW(), NOW())
+ON CONFLICT (id) DO NOTHING;


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

diff --git a/src/utils/helpers/sale.utils/sale-money.ts b/src/utils/helpers/sale.utils/sale-money.ts
index 96f148bc..855eed69 100644
--- a/src/utils/helpers/sale.utils/sale-money.ts
+++ b/src/utils/helpers/sale.utils/sale-money.ts
@@ -13,6 +13,7 @@ import {
   freelancerDocumentsClearForPayment,
 } from 'src/sales/partner-payments/helpers/rules';
 import { calculateCollectedPercentage } from './sale-collected-percentage';
+import { PAYMENT_THRESHOLDS } from 'src/sales-reasons/reasons-enum';
 
 /* RULES */
 
@@ -178,9 +179,16 @@ export function getSaleEffectivePaymentPercentage(
 
   const paidCommPercent = paidNet.divide(totalNet).multiply(100).value;
 
-  // CASE A: remaining commission takes precedence
+  // CASE A: has previous payments — use variance when collected > paid
   if (paidCommPercent > 0) {
-    const effectivePercent = Math.ceil(100 - paidCommPercent);
+    const variance = Math.min(100, collectedPercentage) - paidCommPercent;
+    if (variance >= PAYMENT_THRESHOLDS.REMAINING_UPON_COLLECTION_VARIANCE) {
+      return {
+        effectivePercent: variance,
+        effectiveReason: 'remaining',
+      };
+    }
+    const effectivePercent = 100 - paidCommPercent;
     return { effectivePercent, effectiveReason: 'remaining' };
   }
 
---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1747-Partners-Cycle-Extraction-Collection-Percentage

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/utils/helpers/sale.utils/sale-money.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
new file mode 100644
index 00000000..e5a4d919
--- /dev/null
+++ b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
@@ -0,0 +1,5 @@
+INSERT INTO sales_reasons_ids (id, reason, created_at, updated_at)
+VALUES
+  (19, 'Remaining Upon Collection', NOW(), NOW()),
+  (20, 'Missing Net Amount', NOW(), NOW())
+ON CONFLICT (id) DO NOTHING;


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

diff --git a/src/utils/helpers/sale.utils/sale-money.ts b/src/utils/helpers/sale.utils/sale-money.ts
index 96f148bc..855eed69 100644
--- a/src/utils/helpers/sale.utils/sale-money.ts
+++ b/src/utils/helpers/sale.utils/sale-money.ts
@@ -13,6 +13,7 @@ import {
   freelancerDocumentsClearForPayment,
 } from 'src/sales/partner-payments/helpers/rules';
 import { calculateCollectedPercentage } from './sale-collected-percentage';
+import { PAYMENT_THRESHOLDS } from 'src/sales-reasons/reasons-enum';
 
 /* RULES */
 
@@ -178,9 +179,16 @@ export function getSaleEffectivePaymentPercentage(
 
   const paidCommPercent = paidNet.divide(totalNet).multiply(100).value;
 
-  // CASE A: remaining commission takes precedence
+  // CASE A: has previous payments — use variance when collected > paid
   if (paidCommPercent > 0) {
-    const effectivePercent = Math.ceil(100 - paidCommPercent);
+    const variance = Math.min(100, collectedPercentage) - paidCommPercent;
+    if (variance >= PAYMENT_THRESHOLDS.REMAINING_UPON_COLLECTION_VARIANCE) {
+      return {
+        effectivePercent: variance,
+        effectiveReason: 'remaining',
+      };
+    }
+    const effectivePercent = 100 - paidCommPercent;
     return { effectivePercent, effectiveReason: 'remaining' };
   }
 
---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1747-Partners-Cycle-Extraction-Collection-Percentage

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/utils/helpers/sale.utils/sale-money.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
new file mode 100644
index 00000000..e5a4d919
--- /dev/null
+++ b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
@@ -0,0 +1,5 @@
+INSERT INTO sales_reasons_ids (id, reason, created_at, updated_at)
+VALUES
+  (19, 'Remaining Upon Collection', NOW(), NOW()),
+  (20, 'Missing Net Amount', NOW(), NOW())
+ON CONFLICT (id) DO NOTHING;


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

diff --git a/src/utils/helpers/sale.utils/sale-money.ts b/src/utils/helpers/sale.utils/sale-money.ts
index 96f148bc..855eed69 100644
--- a/src/utils/helpers/sale.utils/sale-money.ts
+++ b/src/utils/helpers/sale.utils/sale-money.ts
@@ -13,6 +13,7 @@ import {
   freelancerDocumentsClearForPayment,
 } from 'src/sales/partner-payments/helpers/rules';
 import { calculateCollectedPercentage } from './sale-collected-percentage';
+import { PAYMENT_THRESHOLDS } from 'src/sales-reasons/reasons-enum';
 
 /* RULES */
 
@@ -178,9 +179,16 @@ export function getSaleEffectivePaymentPercentage(
 
   const paidCommPercent = paidNet.divide(totalNet).multiply(100).value;
 
-  // CASE A: remaining commission takes precedence
+  // CASE A: has previous payments — use variance when collected > paid
   if (paidCommPercent > 0) {
-    const effectivePercent = Math.ceil(100 - paidCommPercent);
+    const variance = Math.min(100, collectedPercentage) - paidCommPercent;
+    if (variance >= PAYMENT_THRESHOLDS.REMAINING_UPON_COLLECTION_VARIANCE) {
+      return {
+        effectivePercent: variance,
+        effectiveReason: 'remaining',
+      };
+    }
+    const effectivePercent = 100 - paidCommPercent;
     return { effectivePercent, effectiveReason: 'remaining' };
   }
 
---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1747-Partners-Cycle-Extraction-Collection-Percentage

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/utils/helpers/sale.utils/sale-money.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
new file mode 100644
index 00000000..e5a4d919
--- /dev/null
+++ b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
@@ -0,0 +1,5 @@
+INSERT INTO sales_reasons_ids (id, reason, created_at, updated_at)
+VALUES
+  (19, 'Remaining Upon Collection', NOW(), NOW()),
+  (20, 'Missing Net Amount', NOW(), NOW())
+ON CONFLICT (id) DO NOTHING;


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

diff --git a/src/utils/helpers/sale.utils/sale-money.ts b/src/utils/helpers/sale.utils/sale-money.ts
index 96f148bc..855eed69 100644
--- a/src/utils/helpers/sale.utils/sale-money.ts
+++ b/src/utils/helpers/sale.utils/sale-money.ts
@@ -13,6 +13,7 @@ import {
   freelancerDocumentsClearForPayment,
 } from 'src/sales/partner-payments/helpers/rules';
 import { calculateCollectedPercentage } from './sale-collected-percentage';
+import { PAYMENT_THRESHOLDS } from 'src/sales-reasons/reasons-enum';
 
 /* RULES */
 
@@ -178,9 +179,16 @@ export function getSaleEffectivePaymentPercentage(
 
   const paidCommPercent = paidNet.divide(totalNet).multiply(100).value;
 
-  // CASE A: remaining commission takes precedence
+  // CASE A: has previous payments — use variance when collected > paid
   if (paidCommPercent > 0) {
-    const effectivePercent = Math.ceil(100 - paidCommPercent);
+    const variance = Math.min(100, collectedPercentage) - paidCommPercent;
+    if (variance >= PAYMENT_THRESHOLDS.REMAINING_UPON_COLLECTION_VARIANCE) {
+      return {
+        effectivePercent: variance,
+        effectiveReason: 'remaining',
+      };
+    }
+    const effectivePercent = 100 - paidCommPercent;
     return { effectivePercent, effectiveReason: 'remaining' };
   }
 
---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1747-Partners-Cycle-Extraction-Collection-Percentage

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/utils/helpers/sale.utils/sale-money.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
new file mode 100644
index 00000000..e5a4d919
--- /dev/null
+++ b/database/migrations/20260407120000_seed_sales_reasons_ids_collection_percentage/migration.sql
@@ -0,0 +1,5 @@
+INSERT INTO sales_reasons_ids (id, reason, created_at, updated_at)
+VALUES
+  (19, 'Remaining Upon Collection', NOW(), NOW()),
+  (20, 'Missing Net Amount', NOW(), NOW())
+ON CONFLICT (id) DO NOTHING;


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

(no changes in this PR)


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no
## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1014)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1747)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
