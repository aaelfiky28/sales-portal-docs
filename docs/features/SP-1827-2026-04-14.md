---
title: "SP-1827 - SP-1827-Ymir-Integration-for-different-SMS-providers-and-Automatic-Triggers"
date: 2026-04-14
author: github-actions[bot]
pr_number: 1092
jira_ticket: SP-1827
---

## 📌 PR Overview

* Title: SP-1827-Ymir-Integration-for-different-SMS-providers-and-Automatic-Triggers
* Type: feature
* Scope: small

---

## 🧠 Summary of Changes

* Bullet points of ACTUAL changes (not generic)

---

## 🏗️ Architecture Impact

* Layers Modified: Prisma, DTOs, Services, etc.
* Flow Impact: Controller → Service → Repository → DB (only what the diff shows)

---

## 📂 Detailed Changes

### Prisma Models & DB (migrations + schema.prisma)

* List added/updated columns/fields with SQL/Prisma names
* Defaults, nullability, and purpose where inferable

### DTOs

* New/changed fields, decorators/validation if visible
* Impact on API input shape

### Services

For each touched service file:

* Conditions/branches added or changed
* Methods or calls introduced (name them)
* BEFORE vs AFTER behavior

### Controllers

* Explicitly state "No changes" if the diff shows none

### Repositories

* Explicitly state "No changes" if the diff shows none

### Helpers

* Explicitly state "No changes" if the diff shows none

---

## 🔄 Data Flow Changes

Explain how data flow changed across: Controller → Service → Repository → DB (only what the diff supports).

---

## ⚠️ Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1827-Ymir-Integration-for-different-SMS-providers-and-Automatic-Triggers

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
.env.example
src/sms/sms.service.ts
src/topics/types/notification.enum.ts
src/ymir/constants.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

(no changes in this PR)


## Prisma schema (database/schema/schema.prisma)

(no changes in this PR)


## Controllers (*.controller.ts)

(no changes in this PR)


## Services (*.service.ts)

diff --git a/src/sms/sms.service.ts b/src/sms/sms.service.ts
index 25fc733e..6c4bbd93 100644
--- a/src/sms/sms.service.ts
+++ b/src/sms/sms.service.ts
@@ -5,6 +5,8 @@ import { InjectAwsService } from 'nest-aws-sdk';
 import { LoggerHandlerService } from 'src/common/logger-handler/logger-handler.service';
 import { LogSeverity, TargetAudience } from 'src/common/types/logger.type';
 import { UserEntity } from 'src/users/entities/user.entity';
+import { NotificationTypes } from 'src/topics/types/notification.enum';
+import { YMIR_NOTIFICATION_PORTAL, YMIR_NOTIFICATION_SENDER, YMIR_TEMPLATE_NAMES } from 'src/ymir/constants';
 
 @Injectable()
 export class SmsService {
@@ -12,28 +14,36 @@ export class SmsService {
     @InjectAwsService(SNS)
     private snsService: SNS,
     private readonly loggerService: LoggerHandlerService,
-  ) {}
+  ) { }
 
   async sendCommissionAvailableMessage(
     users: UserEntity[],
     unitNumber: string,
     externalSystemUrl: string,
   ): Promise<void> {
-    if (process.env.SEND_MAILS !== 'true') return;
     for (const user of users) {
       const { phone } = user;
       if (phone) {
         let formattedPhone = this.getFormattedPhoneNumber(phone);
-        const message = `Approving total commission for Unit Number ${unitNumber} is now available you can approve it here: ${externalSystemUrl}, Thank you.`;
 
+        const payload = {
+          name: YMIR_TEMPLATE_NAMES.commissionApprovalAvailable,
+          portal: YMIR_NOTIFICATION_PORTAL,
+          sender: YMIR_NOTIFICATION_SENDER,
+          phones: [formattedPhone],
+          variables: { unitNumber, externalSystemUrl },
+        };
         const messageParams = {
-          Message: JSON.stringify({ phone: formattedPhone, message }),
+          Message: JSON.stringify(payload),
           MessageGroupId: '1',
           MessageDeduplicationId: randomUUID(),
           MessageAttributes: {
-            title: { DataType: 'String', StringValue: 'sms' },
+            title: {
+              DataType: 'String',
+              StringValue: NotificationTypes.ymirNotificationTitle,
+            },
           },
-          TopicArn: process.env.AWS_SMS_SNS_ARN,
+          TopicArn: process.env.AWS_YMIR_NOTIFICATION_SNS_ARN,
         };
 
         this.snsService.publish(messageParams, (error, result) => {
@@ -63,18 +73,27 @@ export class SmsService {
   }
 
   async sendOtpMessage(otpCode: string, phone: string): Promise<void> {
-    if (process.env.SEND_MAILS !== 'true') return;
     let formattedPhone = this.getFormattedPhoneNumber(phone);
-    const message = `Your login verification code for Nawy Sales Portal is ${otpCode}, For your security, please do not share this code with anyone`;
+
+    const payload = {
+      name: YMIR_TEMPLATE_NAMES.midasLoginOtp,
+      portal: YMIR_NOTIFICATION_PORTAL,
+      sender: YMIR_NOTIFICATION_SENDER,
+      phones: [formattedPhone],
+      variables: { otpCode },
+    };
 
     const messageParams = {
-      Message: JSON.stringify({ phone: formattedPhone, message }),
+      Message: JSON.stringify(payload),
       MessageGroupId: '1',
       MessageDeduplicationId: randomUUID(),
       MessageAttributes: {
-        title: { DataType: 'String', StringValue: 'sms' },
+        title: {
+          DataType: 'String',
+          StringValue: NotificationTypes.ymirNotificationTitle,
+        },
       },
-      TopicArn: process.env.AWS_SMS_SNS_ARN,
+      TopicArn: process.env.AWS_YMIR_NOTIFICATION_SNS_ARN,
     };
 
     this.snsService.publish(messageParams, (error, result) => {
```
## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1092)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1827)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
