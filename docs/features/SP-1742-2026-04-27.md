---
title: "SP-1742 - SP-1742-Freelancers-Cycles-Extraction"
date: 2026-04-27
author: github-actions[bot]
pr_number: 1034
jira_ticket: SP-1742
---

## 📌 PR Overview

* Title: SP-1742-Freelancers-Cycles-Extraction
* Type: feature
* Scope: small

---

## 🧠 Summary of Changes

* Bullet points of ACTUAL changes (not generic)

---

## 🏗️ Architecture Impact

* Layers Modified: Prisma, DTOs, Services, etc.
* Flow Impact: What changed along Controller → Service → Repository → DB, as far as the diff shows

---

## 📂 Detailed Changes

### Prisma Models & DB (migrations + schema.prisma)

* List added/updated columns/fields with SQL/Prisma names
* Defaults, nullability, and purpose where inferable

### DTOs

* New/changed fields, decorators/validation if visible
* Impact on API input shape

### Services

For each touched service file:

* Conditions/branches added or changed
* Methods or calls introduced (name them)
* BEFORE vs AFTER behavior

### Controllers

* Explicitly state "No changes" if the diff shows none

### Repositories

* Explicitly state "No changes" if the diff shows none

### Helpers

* Explicitly state "No changes" if the diff shows none

---

## 🔄 Breaking Changes

* Yes / No
* Explain WHY (schema, API contract, behavior)

---

## 📁 Changed Files (Grouped)

### Prisma / DB

### Services

### DTOs

### Others

## INPUT

PR Title:
SP-1742-Freelancers-Cycles-Extraction

Diff range: `origin/master...HEAD` (base ref: `master`)

Changed paths (for grouping; do not copy this list as your whole answer):
database/migrations/20260310120000_add_promo_freelancers_express_promo_h1_2026/migration.sql
database/migrations/20260407120100_seed_sales_reasons_invalid_commission_type_freelancer/migration.sql
database/schema/enums.prisma
src/sales-reasons/reasons-enum.ts
src/sales-reasons/sales-reasons-beta-old.service.ts
src/sales-reasons/sales-reasons.service.beta.ts
src/sales-reasons/sales.reasons.service.ts

---

PR Diff:



## SQL migrations (database/migrations/**/migration.sql)

diff --git a/database/migrations/20260310120000_add_promo_freelancers_express_promo_h1_2026/migration.sql b/database/migrations/20260310120000_add_promo_freelancers_express_promo_h1_2026/migration.sql
new file mode 100644
index 00000000..51ff2562
--- /dev/null
+++ b/database/migrations/20260310120000_add_promo_freelancers_express_promo_h1_2026/migration.sql
@@ -0,0 +1,2 @@
+-- AlterEnum
+ALTER TYPE "PartnerCommissionTypes" ADD VALUE 'PromoFreelancersExpressPromoH12026';
diff --git a/database/migrations/20260407120100_seed_sales_reasons_invalid_commission_type_freelancer/migration.sql b/database/migrations/20260407120100_seed_sales_reasons_invalid_commission_type_freelancer/migration.sql
new file mode 100644
index 00000000..c2cc4f0c
--- /dev/null
+++ b/database/migrations/20260407120100_seed_sales_reasons_invalid_commission_type_freelancer/migration.sql
@@ -0,0 +1,3 @@
+INSERT INTO sales_reasons_ids (id, reason, updated_at, created_at)
+VALUES (21, 'Invalid Commission Type for Freelancer', now(), now())
+ON CONFLICT (reason) DO NOTHING;
diff --git a/src/sales-reasons/reasons-enum.ts b/src/sales-reasons/reasons-enum.ts
index 65f8c740..0fa9297f 100644
--- a/src/sales-reasons/reasons-enum.ts
+++ b/src/sales-reasons/reasons-enum.ts
@@ -36,7 +36,11 @@ import { ENTITYNAMES } from 'src/sales/types/entity-names.enum';
 import { calculateCollectedPercentage } from './helpers/calculate-collected-percentage';
 import { calculateCommissionAmounts } from './helpers/calculate-paid-commission-percentage copy';
-import { PAYMENT_THRESHOLDS, SALES_REASONS_IDS } from './reasons-enum';
+import {
+  FREELANCER_ALLOWED_COMMISSION_TYPES,
+  PAYMENT_THRESHOLDS,
+  SALES_REASONS_IDS,
+} from './reasons-enum';
 const BATCH_SIZE = 200;
 //  dynamic rules defaults
 const DEFAULT_RULES = {
@@ -64,6 +68,9 @@ const COMMISSION_TYPES = {
     PartnerCommissionTypes.October24Promo,
     PartnerCommissionTypes.ExpressFestivalH2,
     PartnerCommissionTypes.CityScapePromo25,
+    PartnerCommissionTypes.FreelancerFirstSale,
+    PartnerCommissionTypes.FreelancersPromo,
+    PartnerCommissionTypes.PromoFreelancersExpressPromoH12026,
   ] as PartnerCommissionTypes[],
   PROMO_TYPES: [
     PartnerCommissionTypes.Express,
@@ -74,6 +81,8 @@ const COMMISSION_TYPES = {
     PartnerCommissionTypes.October24Promo,
     PartnerCommissionTypes.ExpressFestivalH2,
     PartnerCommissionTypes.CityScapePromo25,
+    PartnerCommissionTypes.FreelancersPromo,
+    PartnerCommissionTypes.PromoFreelancersExpressPromoH12026,
   ] as Partial<PartnerCommissionTypes[]>;
 
@@ -1025,6 +1034,17 @@ export class SalesReasonsService {
             continue; // Skip all other checks
           }
 
+          // Freelancer sales: only Normal, Express, FreelancerFirstSale, FreelancersPromo are applicable
+          if (
+            sale?.tierId &&
+            FREELANCER_TIERS.includes(sale.tierId) &&
+            sale.partnerCommissionType &&
+            !FREELANCER_ALLOWED_COMMISSION_TYPES.includes(
+              sale.partnerCommissionType,
+            )
+          ) {
+            reasons.add(SALES_REASONS_IDS.INVALID_FREELANCER_COMMISSION_TYPE);
+            canBeEligible = false;
+          }
+
           if (!sale.partnerCommissionPercentage) {
             reasons.add(SALES_REASONS_IDS.NO_COMMISSION); // No Commission %
             canBeEligible = false;
         }


## Repositories (*.repository.ts)

(no changes in this PR)


## DTOs (*.dto.ts)

(no changes in this PR)


## Helpers (*.helper.ts and */helpers/*.ts)

(no changes in this PR)

## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/1034)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1742)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
