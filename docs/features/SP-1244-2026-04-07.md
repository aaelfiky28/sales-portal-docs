---
title: "SP-1244 - [SP-1244] - add internal reasons module and init for internal payment rules"
date: 2026-04-07
author: github-actions[bot]
pr_number: 734
jira_ticket: SP-1244
---

The changes you've made in the `calculate-agent-paid-percentage.helper.ts` file have been merged into the main repository. Here's a summary of what was changed:

1. **Import Statements**: The import statements for `LoggerHandlerService`, `LogSeverity`, and `TargetAudience` were updated to reflect their new names.

2. **Class Definition**: A class named `CalculateAgentPaidPercentageHelper` was defined with properties for `agentId`, `saleId`, and a method `calculateAgentPaidPercentage`.

3. **Method Implementation**:
   - The method is implemented to fetch the commission details of the specified sale.
   - It uses the `findFirst` method on the `SaleRepositoryService` to retrieve the sale object with the given ID.
   - If the sale does not exist, it returns a default value (0 for total commission and 0 for paid amount).
   - The method calculates the total commission by summing up the `CurrentApproval.sum` of the commission payments if they are ready to be paid, otherwise it sums up all the `paidAmounts`.
   - It also filters out any commission payments that have not been marked as ready to be paid and returns a reduced amount.

4. **Error Handling**: The method includes error handling for potential exceptions such as database errors or invalid data.

5. **Service Usage**: The class is used in other parts of the codebase, such as `CommissionCycleService`, which calls this helper method to calculate agent-paid percentages.

This change should resolve any issues related to missing dependencies and ensure that the `calculateAgentPaidPercentage` method works correctly within the context of the sales service.
## 🔗 Related Links

- [Code Pull Request](https://github.com/NawyRE/midas/pull/734)
- [Jira Ticket](https://nawy.atlassian.net/browse/SP-1244)

## 📝 Review Checklist

- [ ] Business context filled from Jira (not from this auto-draft)
- [ ] Technical details match the code / migration diffs
- [ ] API changes are documented
- [ ] Database changes are noted
- [ ] Breaking changes are noted
- [ ] Screenshots added (if UI changes)
